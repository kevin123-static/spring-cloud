server:
  port: 8888
spring:
  application:
    name: config-server
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/kevin123-static/spring-cloud.git
          username: kevin123-static
          password: likunwen123
